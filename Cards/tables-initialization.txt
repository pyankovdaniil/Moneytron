create table card(
    id integer generated by default as identity primary key,
    ownerEmail varchar not null unique,
    cardName varchar not null unique,
    constraint unique_cardName_ownerEmail unique (ownerEmail, cardName),
    balance float8 not null
);